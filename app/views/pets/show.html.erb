<h1><%= @pet.name %></h1>
<img class="pet-show-img"src="<%= @pet.image_path %>" alt="<%= @pet.image_path %>">
<p>Description: <%= @pet.description %> </p>
<p>Approximate Age: <%= @pet.approximate_age %> </p>
<p>Sex: <%= @pet.sex %> </p>
<p>
  Adoption status:
  <%=
    if @pet.adoption_status == true
      "Adoptable"
    else
      "Pending"
    end
  %>
</p>
<p>
<%= if @pet.adoption_status == false
    "#{@pet.name} is on hold for #{@application.find_applicant_name}"
  end
 %>
 </p>



<% if @pet.adoption_status == true %>
  <%= link_to "Change to Adoption Pending", "/pets/#{@pet.id}/pending", method: :patch%>
<% elsif @pet.adoption_status == false %>
  <%= link_to "Change to Adoptable", "/pets/#{@pet.id}/adoptable", method: :patch%>
<% end %>

<% if favorites.contents.any? { |pet_id| pet_id == @pet.id.to_s } %>
  <%= link_to "Remove From Favorites", "/favorites/#{@pet.id}", method: :delete %>
  <% else %>
<%= link_to "Favorite Pet", "/favorites/#{@pet.id}", method: :patch %>
<% end %>

<%= link_to "View All Applications", "/pets/#{@pet.id}/applicatons", id: "applications-#{@pet.id}" %>
<%= link_to "Update Pet", "/pets/#{@pet.id}/edit", id: "update-pet-#{@pet.id}" %>
<%= button_to "Delete Pet", "/pets/#{@pet.id}", method: :delete %>
